@model WebSigma.Models.ViewModels.VisitorViewModel

@{
    ViewBag.Title = "Index";

}
<script>
    
        
</script>

<div class="row justify-content-center">
    <div>

    </div>
    <div class="col-lg-8 text-center">
        <div>
            <img src="~/Images/sigma-logo.png" class="img-fluid" alt="logo">
        </div>
        <br />
        <h2 style="font-weight:700">Prueba de desarrollo Sigma</h2>
        <br />
        <p style="color:#747172; font-weight:700">
            Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever
            since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book.
        </p>
    </div>

</div>

<div class="row justify-content-center" style="margin-top:35px;">
    <div class="col-lg-5">
        <img src="~/Images/sigma-image.png" class="img-fluid" alt="logo">
    </div>
    <div class="col-lg-5">
        <div class="card formulario" >
            <div class="card-body">
                <form action="Nuevo" method="post">
                    @*@using (Html.BeginForm("Nuevo", "Visitor", FormMethod.Post))*@
                    {
                    <div class="form-group">
                        @Html.DropDownList("state",
                            ViewBag.States as SelectList,
                            "Please select",
                            new { id = "state" })
                        <select class="form-control" id="State" size="1">
                            <option value="uno" selected="selected">uno</option>
                        </select>
                    </div>
                    <div class="form-group">
                        @Html.DropDownList("city",
                            ViewBag.States as SelectList,
                            "Please select",
                            new { id = "city" })
                    </div>
                        <div class="form-group">
                            <strong>@Html.LabelFor(d => d.Name)</strong>
                            @Html.TextBoxFor(d => d.Name, "Nombre*", new { @class = "form-control" })
                            @Html.ValidationMessageFor(d => d.Name, null, new { @class = "danger" })
                        </div>
                        <div class="form-group">
                            <strong>@Html.LabelFor(d => d.Email)</strong>
                            @Html.TextBoxFor(d => d.Email, "Correo*", new { @class = "form-control" })
                            @Html.ValidationMessageFor(d => d.Email, null, new { @class = "danger", placeholder = "pepitodejesus@gmail.com" })

                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary btn-circle text-center" style="border-color: hotpink;background-color:hotpink; border-radius:15px;" value="enviar">ENVIAR</button>

                        </div>}
                </form>
            </div>
        </div>
    </div>
</div>